# Frontend configuration (Create React App)
# Set this for local development to point at the backend API.
# In production, leave unset to use same-origin (reverse proxy).
REACT_APP_NOTES_API_BASE_URL=http://localhost:3001

